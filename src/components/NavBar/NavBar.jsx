import "./NavBar.scss";

const NavBar = () => {

  return (
    <div className="navbar">
      <button>&lt; Exit</button>
      <button>Save Draft</button>
      <h2>Create New Post</h2>
    </div>
  )
}

export default NavBar;